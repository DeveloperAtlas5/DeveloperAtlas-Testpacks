# Supplied changed-file records

These text files stand in for an exact changed-file list supplied by a coding tool or version-control review. The public Control exercise deliberately keeps this input separate from verification results.
